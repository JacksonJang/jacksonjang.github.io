services:
  blog:
    build: .
    container_name: jacksonjang-blog
    ports:
      - "4000:4000"
      - "35729:35729"
    volumes:
      - .:/srv/jekyll
      - jekyll_bundle:/usr/local/bundle

volumes:
  jekyll_bundle:
